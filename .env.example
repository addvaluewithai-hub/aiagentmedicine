# Server-only. Never rename this to EXPO_PUBLIC_AI_API.
AI_API=

# Client-safe URL for deployed native builds, e.g. https://your-app.expo.app
# Development builds fall back to the Metro host automatically.
EXPO_PUBLIC_API_BASE_URL=
